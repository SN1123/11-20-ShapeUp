// A Circle object represents a circle defined by a center point and a radius.
public class Circle {
  int CenterPoint;
  int radius;
  
  public Circle (int initCenterPoint, int initradius){
    CenterPoint = initCenterPoint;
    radius = initradius;
  }
}
